Config = {}
Config.core = 'qb-core'                         --use your own server core like(qb-core,etc..)
Config.cuffs = 'handcuffs'                        --this the item u need  to be able to open the actions menu
Config.escortevent = 'police:client:EscortPlayer' --if youre using qb-policejob leave it like this else use ur own event
Config.cuffevent = 'police:client:CuffPlayer'     --if youre using qb-policejob leave it like this else use ur own event
Config.putplayerevent = 'police:client:PutPlayerInVehicle' --if youre using qb-policejob leave it like this else use ur own event
Config.putplayeroutevent = 'police:client:SetPlayerOutVehicle' --if youre using qb-policejob leave it like this else use ur own event
Config.robplayerevent = 'police:client:RobPlayer'              --if youre using qb-policejob leave it like this else use ur own event
